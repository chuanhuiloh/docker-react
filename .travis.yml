language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t chuanhuiloh/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true chuanhuiloh/docker-react npm run test -- --coverage

